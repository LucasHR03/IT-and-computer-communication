<!DOCTYPE html>
<html lang="da">
<head>
  <meta charset="UTF-8">
  <title>Log ind | EPJ System</title>
  
  <!-- Opdateret CSS-link -->
  <link rel="stylesheet" href="../public/css/style.css">
  
  <style>
    body {
      background: linear-gradient(135deg, #0891b2, #06b6d4);
      font-family: Arial, sans-serif;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      color: #1e293b;
    }
    .form-container {
      background: white;
      padding: 2rem;
      border-radius: 10px;
      width: 300px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    h1 {
      color: #0891b2;
      margin-bottom: 1rem;
      text-align: center;
    }
    label {
      display: block;
      margin: 0.5rem 0 0.2rem;
    }
    input {
      width: 100%;
      padding: 0.5rem;
      margin-bottom: 1rem;
      border: 1px solid #ccc;
      border-radius: 4px;
    }
    button {
      width: 100%;
      padding: 0.7rem;
      background-color: #0891b2;
      color: white;
      border: none;
      border-radius: 5px;
      font-size: 1rem;
      cursor: pointer;
    }
    button:hover {
      background-color: #066a8b;
    }
    .message {
      color: red;
      text-align: center;
      margin-top: 1rem;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h1>Log ind</h1>
    
    <form method="POST" action="login.psp">
      <label>Brugernavn</label>
      <input type="text" name="username" required>
      
      <label>Adgangskode</label>
      <input type="password" name="password" required>
      
      <button type="submit">Log ind</button>
    </form>

    <%
      # PSP-Pythonkode starter her
      import cgi
      form = cgi.FieldStorage()
      username = form.getfirst("username", "")
      password = form.getfirst("password", "")

      # Kun vis besked, hvis der er indsendt POST
      if req.method == "POST":
          if username == "admin" and password == "1234":
              req.write('<p class="message" style="color:green;">Login lykkedes! 🎉</p>')
          else:
              req.write('<p class="message">Forkert brugernavn eller adgangskode</p>')
    %>
  </div>
</body>
</html>
